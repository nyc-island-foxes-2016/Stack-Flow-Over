<div class="Question">
  <h1><%=@question.body%></h1>
  <p>Posted by: <%=@question.user.name%></p>
  <p>Posted on: <%=std_date_format(@question.created_at) %></p>
</div>
<div>
  <p><%= link_to "<span> &#x25B3; </span>".html_safe, new_question_vote_path(params[:id], vote_type: 1)%> <%= @question.vote_score %>
      <%= link_to "<span> &#x25BD; </span>".html_safe, new_question_vote_path(params[:id], vote_type: -1) %>
  </p>
</div>


  <% if current_user == @question.user %>
    <div id="edit-question">
      <%=link_to "Edit question", edit_question_path %>
    </div>
  <% end %>

  <div class="font-small">
    <%= link_to "Answer this question", new_question_answer_path(params[:id]) %>
  </div>

  <div class="font-small">
    <%= link_to "New Comment", new_question_comment_path(params[:id]) %>
  </div>

<% if !@question.comments.empty? %>
  <div class="comments">
    <h3> Comments: </h3>
    <%= render @question.comments %>
  </div>
<% end %>

<% if !@question.answers.empty? %>
  <div class="answers">
    <h3>Answers: </h3>
    <%= render @question.answers %>
  </div>
<% end %>
